# タグ付け作業の引き継ぎ

**やってほしいこと: 車載写真7,539枚に、写っている植物のタグを付ける。**

Claude Code に丸ごと任せられる作業です。人が画像を見る必要はありません。

---

## 何をしているプロジェクトか

GeoGuessr（ストリートビューだけで場所を当てるゲーム）の学習教材を作っています。

Mapillary の車載写真に**写っている植物にタグを付けて**、
「世界の道端で実際に何が見えるか」を実測しています。

- 第1弾: 世界からランダムに10,001枚 → **完了**（前回お願いした分）
- 第2弾: **GeoGuessr のカバー104カ国それぞれで最低30点**を確保するため
  国ごとに追加収集 → 収集は完了、**タグ付けが7,539枚残っている** ← ここをお願いしたい

前回に続いてありがとうございます。

成果物: https://dmkn013.github.io/geoguessr-flora/

---

## セットアップ

画像は **untagged ブランチ** に置いてあります（675MB）。
main の履歴を汚さないため別ブランチにしています。

```bash
git clone https://github.com/dmkn013/geoguessr-flora.git
cd geoguessr-flora
git checkout untagged -- data/untagged
pip install pillow
```

`git checkout untagged -- data/untagged` で画像だけを取り出せます
（ブランチを切り替えるのではなく、main にいるまま画像を持ってくる形）。

依存は Pillow だけです。

動作確認:

```bash
python src/tag_sheet.py stats
```

`タグ付け 10682 / 収集 17970枚` と出れば準備完了です。

---

## Claude Code への依頼文

Claude Code を起動して、**以下をそのまま貼ってください。**

7,539枚あるので、3つのターミナル（またはタブ）で `--w2` `--w3` `--w4` を
並列に走らせると3倍速くなります。1つだけでも動きます。

````
GeoGuessr学習教材の画像タグ付けです。日本語で回答してください。

**車載写真に写っている植物のタグを付けてください。**「完了:」が出るか、最大100ページ処理したら報告して終了。

重要: コマンドには必ず `--w2` を付けてください（並列実行のため）。

手順（「完了:」が出るまで繰り返す）:

1. `python src/tag_sheet.py --w2 next`
→「完了:」なら終了。画像パスが出たら次へ。

2. Readツールでその画像を開く（dist/tag_sheet2.png）。**4枚**、左上に通し番号（0始まり）と座標。

3. 各画像について、**属を特定できる植物**をタグにする。

**粒度: 属レベルを基本とし、種まで分かれば種も付ける。**
科レベルは原則使わない。ただし**ヤシ科・タケ亜科は例外**（属の判別が実際に困難なため）。

**付けてよいもの**: 分類群の名前（マツ属、ユーカリ属、ネムノキ属…）
**付けてはいけないもの**: 植生タイプ（針葉樹林、広葉樹林、草地、農地、砂漠…）

**新しい植物は自由にタグを作ってよい**。下のリストは表記を揃えるための参考であって、
リストに無いから見送るのは誤りです。ただし**日本語の分類名**で書いてください
（Royal palm ではなく「ダイオウヤシ属」）。

既存タグ（同じものならこの表記に正確に揃える。バナナの株は「バショウ属」）:
マツ属 / ユーカリ属 / ヤシ科 / トウヒ属 / カバノキ属 / バショウ属 / ココヤシ / アカシア属 / ポプラ属 / ネムノキ属 / オウギヤシ属 / コナラ属 / イトスギ属 / シラカンバ / マンゴー属 / アブラヤシ属 / サトウキビ / ビャクシン属 / バナナ / タケ亜科 / オリーブ属 / トウモロコシ / ヨーロッパアカマツ / アラウカリア属 / カラマツ属 / ナツメヤシ属 / ヨモギ属 / ヤナギ属 / ウチワサボテン属 / セイヨウハコヤナギ / ブナ属 / スギ属 / リュウゼツラン属 / バオバブ属 / プルメリア属 / パパイヤ属 / イチジク属 / ゴムノキ属 / ハシラサボテン類 / アレカヤシ属 / ユッカ属 / イタリアカサマツ / カエデ属 / レインツリー / タコノキ属 / キョウチクトウ属 / ベンガルボダイジュ / ダイオウヤシ属 / コルタデリア属 / ホウオウボク属 / サトウヤシ属 / コルディリネ属 / ユーフォルビア属 / レモンユーカリ / トネリコ属 / クスノキ / モクマオウ属 / パラゴムノキ / イタリアイトスギ

**方針:**
- 1枚に複数タグ可。写っているものは全部書く。
- **迷ったら書かない**。落葉した雑木林など**属を特定できないものは none**。
  植物が茂っていても属が分からなければ none で正しいです。
- 座標は参考にしてよいが、座標だけを根拠にタグを付けない。
- 道路・空だけ、市街地、霞んで不明、暗すぎる、も none。

4. Bashで記録（必ず --w2 を付ける）。**set は1画像につき1コマンド。タグは半角スペース区切り**:

`python src/tag_sheet.py --w2 set 3 バショウ属 バナナ`

番号をまとめてよいのは none だけです:
`python src/tag_sheet.py --w2 none 0 1 2`

**4枚すべてについて set か none のどちらかを必ず実行すること。**

**注意: data/tags.json を直接編集しないでください。** 並列実行しているので、スクリプト経由でのみ書き込んでください。

5. 手順1に戻る。

最後に「Nページ処理 / タグ付けM枚 / うち none K枚」と主なタグを報告。新規に作ったタグがあれば明記してください。
````

2本目・3本目を走らせる場合は、上の文の `--w2` を **すべて** `--w3` / `--w4` に
置き換えてください（`tag_sheet2.png` も `tag_sheet3.png` / `tag_sheet4.png` に）。

---

## 進捗の確認

```bash
python src/tag_sheet.py stats
```

## 【重要】作業を終えたら必ず push してください

**これをしないと結果が戻りません。** 区切りをつけるたび、または最後に必ず:

```bash
python src/normalize_tags.py    # タグの表記ゆれを統合
git add data/tags.json
git commit -m "タグ付け: 〇〇枚まで完了"
git push
```

**`data/untagged/` は add しないでください。** untagged ブランチにあるので、
main に入れるとリポジトリが膨らみます（.gitignore してあります）。

**途中で止めても構いません。** その時点までを push してもらえれば、
そこから引き継げます。**こまめな push を推奨します**（1,000枚ごとくらい）。

状態は `data/tags.json` に1枚ごと保存されるので、
`python src/tag_sheet.py --w2 next` を叩けば続きから再開できます。

### push が競合したら

```bash
git pull --no-rebase        # マージを試みる
```

`data/tags.json` が衝突したら、**両方のタグを残す**のが正解です。
JSON の中身は `画像ID: [タグ...]` の辞書なので、
どちらか一方のエントリを消さないようマージしてください。
判断に迷ったら push せず、依頼主に連絡してください。

---

## 判定の基準（迷ったとき）

**「属レベルを基本、分かれば種も」** が原則です。

| 状況 | タグ |
|---|---|
| 北欧の松、幹の上部がオレンジ | `マツ属 ヨーロッパアカマツ` |
| 幹が白く黒い横縞 | `カバノキ属 シラカンバ` |
| 遠景で松としか分からない | `マツ属` |
| ヤシだが属を特定できない | `ヤシ科`（例外的に科でOK） |
| 竹林 | `タケ亜科`（例外的に科でOK） |
| 落葉した雑木林、何の木か分からない | `none` |
| 道路と空だけ | `none` |

**却下してよい典型例**（実際によくあります）:
- 遠景のシルエットだけで属が分からない混合林
- **葉が落ちた温帯の落葉樹林**（ヨーロッパに多い。木は茂っていても属は分からない）
- 北米東部の落葉樹林、アフリカのミオンボ林
- 逆光・夜間・霞・強いブレ
- 市街地、トンネル内、車内から撮った壁紙や地図画面

無理にタグを付けるとデータが汚れるので、**迷ったら none** で構いません。
実績では**約70%が none** になっています。これは「世界の道端に植物が無い」
のではなく、**「属を特定できる植物が写っていない」**という意味です。

---

## 既知の落とし穴

Claude が踏みやすいので、依頼文に対策を入れてあります。参考まで。

1. **`set 2 3 6 8 タグ名`** と番号をまとめて書く誤用 → スクリプト側で拒否します
2. **カンマ区切り**（`ユーカリ属,バショウ属`）→ 分割処理を入れてあります
3. **英名で書く**（Royal palm）→ 依頼文で日本語の分類名を指定しています
4. **「リストに無いから」と識別できた植物を見送る** → 依頼文で明示的に否定しています
5. **サブエージェントに委譲しようとする** → 前回1体が勝手に委譲しました。
   委譲されても動きますが、報告が分かれます

## 参考

- 詳しい経緯: `docs/` の日付順ハンドオフ
- 現時点の成果物: https://dmkn013.github.io/geoguessr-flora/

---

## 【依頼主向け】結果の受け取り方

作業してもらった側が push したら、こちらで:

```bash
cd /c/Users/shun/OneDrive/work/geoguessr-flora
git pull
python src/tag_sheet.py stats      # 増えているか確認
python src/normalize_tags.py       # 表記ゆれを統合
```

### 注意: こちらでもタグ付けを再開する場合

**同じ画像を両方でタグ付けすると衝突する。** 分担するなら:

- 相手に任せている間は、こちらでは `tag_sheet.py` を動かさない
- または pull してから再開する

`data/tags.json` は画像IDをキーにした辞書なので、
別々の画像を触っている限り git のマージで問題なく統合される。

### タグ付けが全部終わったら

untagged ブランチは不要になるので消してよい:

```bash
git push origin --delete untagged
rm -rf data/untagged
```
